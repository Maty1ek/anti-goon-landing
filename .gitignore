node_modules/
.next/
out/
.DS_Store
*.log
.env*.local

# Binary distribution artifacts — too large for GitHub and not part of
# the deploy. The real DMG lives on a GitHub Release; this folder is
# only used for local-only testing of /install.
public/*.dmg
